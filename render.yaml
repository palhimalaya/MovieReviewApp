databases:
  - name: postgres
    ipAllowList: []
    databaseName: movie_review_zvld
    user: movie_review_zvld_user

services:
  - type: web
    name: movie_review_app_render
    env: ruby
    buildCommand: "./render-build.sh"
    startCommand: "bundle exec rails s"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: postgres
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
